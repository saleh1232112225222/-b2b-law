# تدقيق تنفيذ خطة «تطوير حفظ البيانات» — الحالة الفعلية

> تاريخ التدقيق: 2026-08-31 (Asia/Riyadh)  
> نقطة الفحص: `G:\w2w` عند commit `685cf1b` مع تغييرات مساحة العمل غير المثبتة، و`G:\b2b` بحالته الحالية.  
> المصدر الحاكم: ملف `تطوير حفظ البيانات .md` نفسه، ثم الكود الفعلي والاختبارات المنفذة. لم تُستخدم تقارير R1–R7 دليلاً على الاكتمال.

## 1. الحكم التنفيذي الصريح

**الخطة غير مكتملة وليست مقبولة بنسبة 100%.**

- تقدير الإنجاز الهندسي الموزون للمراحل 0–12: **نحو 63%**.
- المتبقي الهندسي التقريبي: **37%**، لكنه يضم بوابات قبول حرجة ولا يعادل مجرد تنسيق أو تحسينات ثانوية.
- اكتمال Definition of Done: **شرط واحد مكتمل بثقة من 15**، و**14 شرطاً غير مكتملة أو غير مثبتة**.
- حكم الإطلاق: **غير جاهز للإنتاج، وغير مسموح وصفه بأنه “مكتمل” أو “fully backed up”.**

الفرق بين النسبتين مقصود: نسبة 63% تقيس ما بُني من مكونات، أما Definition of Done فهي بوابة تراكمية؛ فشل شرط إلزامي واحد يمنع إغلاق الخطة، وهنا توجد عدة شروط غير متحققة.

## 2. حدود الإثبات ونسبة الأعمال إلى أصحابها

لا يمكن إثبات «ما قام به المستخدم» مقابل «ما قام به الوكيل السابق» بدقة، للأسباب التالية:

1. المستودع عند `main/685cf1b` يحتوي على **70 مدخلاً معدلاً أو غير متتبع** في مساحة العمل.
2. أغلب ملفات الخطة الجديدة غير مثبتة في commits منفصلة؛ لذلك لا توجد سلسلة تاريخية موثوقة تنسب كل سطر لشخص بعينه.
3. `git diff` يثبت الفرق بين commit الحالي ومساحة العمل فقط، ولا يثبت هوية من كتب التغيير.

التصنيف المهني الممكن هو:

- **قبل العمل الحالي:** ما هو مثبت في `685cf1b`.
- **التنفيذ الجاري:** كل التغييرات غير المثبتة في `G:\w2w` و`G:\b2b`.
- **الكاتب الفعلي:** غير قابل للإثبات دون commits أو سجل جلسات موثوق؛ لن يُختلق له إسناد.

لم يُنفذ أثناء هذا التدقيق `reset` أو `clean` أو حذف لملفات المستخدم. التعديل الوحيد داخل المصدر هو إنشاء تقرير التدقيق هذا؛ كما أُنشئت صورة Docker اختبارية اسمها `w2w-backend-dr-audit` دون إعادة إنشاء backend أو volume قاعدة البيانات.

## 3. نتائج البوابات المنفذة فعلياً

| البوابة | النتيجة الفعلية | الحكم |
|---|---:|---|
| تطابق العقود `npm run contracts:check` | فشل | `streamingCrypto.ts` مختلف بين المصدر وWindows، و`recoveryArchive.ts` مختلف بين المصدر والخادم |
| Web TypeScript | نجح | `vue-tsc` exit 0 |
| Web production build | نجح | Vite build exit 0 |
| Web tests | **فشل** | 127 نجح، 1 فشل بسبب timeout في `canonicalContract.test.ts:822` |
| Web ESLint | نجح مع ديون تنسيق | 0 errors، 111 warnings |
| Cloud TypeScript/build | نجح | `tsc` والبناء exit 0 |
| Cloud tests المعتادة | نجحت جزئياً | 38 نجح، 2 skipped؛ الاختباران الأصليان لـ PostgreSQL لا يعملان افتراضياً بلا متغيرات البيئة |
| Cloud ESLint | نجح مع دين كبير | 0 errors، 11275 warnings |
| Live PostgreSQL DR داخل Docker | **نجح فعلياً** | 1/1: dump + restore إلى قاعدة جديدة + جداول + PK/FK/unique + row counts + attachment bytes |
| Desktop TypeScript | نجح | node + web typecheck exit 0 |
| Desktop production build | نجح بعد منح صلاحية الكتابة | main/preload/renderer build exit 0 |
| Desktop ESLint | نجح مع ديون تنسيق | 0 errors، 385 warnings |
| Desktop tests | **فشل** | 92 نجح، 3 فشل: اختبارا `CloudRestoreService.integration` واختبار `CloudRestoreService.realdata.audit` |
| Docker runtime | يعمل | backend وPostgreSQL بحالة healthy؛ هذا يثبت الإقلاع فقط ولا يثبت اكتمال الخطة |

ملاحظة مهمة: أمر اختبار سطح المكتب أعاد رمز خروج 0 من غلاف PowerShell رغم أن Vitest أعلن 3 حالات فاشلة. الحكم هنا مبني على نتيجة Vitest، لا على رمز الغلاف المضلل.

## 4. مطابقة الهدف الأساسي والقواعد غير القابلة للتفاوض

| بند الخطة | الحالة | الدليل/النقص |
|---|---|---|
| حفظ البيانات والأسماء والمخططات والتغييرات | جزئي | لا توجد عمليات reset/rename مدمرة ظاهرة، لكن لا توجد مقارنة ترحيل كاملة من كل schema قديم ولا clean-upgrade drill |
| دعم JSON القديم | مكتمل وظيفياً بدرجة جيدة | مسارات الاستيراد القديمة باقية؛ توجد اختبارات توافق، لكن لم ينجح فحص دورة استعادة سطح المكتب كاملة في هذه الجولة |
| حزمة `.b2btenant` محمولة ومشفرة | جزئي متقدم | العقد والحزمة والتشفير المتدفق موجودة؛ تطابق نسخ `streamingCrypto/recoveryArchive` فشل، ولا توجد دورة حقيقية كاملة بين التطبيقين |
| Web ↔ Windows recovery الكامل | غير مثبت | لا يوجد اختبار حقيقي ناجح Web→Windows وWindows→Web بكل الكيانات والمرفقات |
| عدم نقل ملفات DB للمزامنة | مكتمل | المزامنة منطقية عبر push/pull ولا تنقل PostgreSQL/SQLite الخام |
| backup وsync عند startup | جزئي | المنسق موصول؛ لا يوجد post-sync encrypted external backup، ولا staging DB كامل للتغييرات الواردة، ولا retry scheduler مثبت |
| منع snapshot injection التلقائي | مكتمل في المسار المرصود | startup يستخدم push/pull/checkpoints ولا يستورد حزمة كاملة تلقائياً |
| offline durable queue | جزئي متقدم | `G:\b2b` يستخدم SQLite؛ تطبيق الويب المشترك ما زال يحتوي `SyncEngineService`/localStorage ومسار نجاح زائف |
| كل الكيانات والمرفقات | غير مثبت | coverage tests واسعة، لكن العقد منجرف والـ round-trip الشامل غير موجود؛ download للمرفقات غير مكتمل |
| منع تسرب المستأجر | جزئي | allowlists وعزل company_id موجودان؛ اختبارات native الشاملة لكل parent/root scope غير مكتملة، وجلسات الاستيراد داخل `Map` لا تناسب replicas |
| استعادة على جهاز Windows مختلف | غير مثبت | التشفير يدعم recovery passphrase نظرياً، لكن لا يوجد clean-machine Windows drill فعلي |

قواعد السلامة الخاصة بعدم لمس production والبيانات الحساسة تم احترامها في هذه المراجعة. أما شرط «كل restore مدمر له backup متحقق وrollback مجرّب» فلا يزال جزئياً بسبب غياب تسوية `COMMIT outcome unknown` وتجربة انقطاع كل مرحلة.

## 5. تقييم المراحل 0–12 بنداً بنداً

### Phase 0 — الجرد والسلامة: 80% (المتبقي 20%)

- **منفذ:** سجل كيانات، عقد مولد، اختبارات تغطية للمخططات، مجلدات اختبار مؤقتة، وتوثيق مخاطر.
- **الدليل:** `src/shared/entityRegistry.ts`، `src/shared/canonicalContract.ts`، `src/renderer/src/__tests__/canonicalContract.test.ts`.
- **النقص:** تعليق السجل يقول 82 كياناً بينما عدّ تعريفات `canonicalName` في المصدر يعطي 81، والعقد المولد يعلن 93 كياناً عابراً للمنصتين. لا توجد مصفوفة موقعة تفسر الفروق بين business/platform/excluded لكل جدول فعلي بعد جميع migrations.
- **بوابة الإكمال:** استخراج schemas الفعلية من PostgreSQL وSQLite بعد migrations ومقارنتها آلياً بالسجل مع تقرير صفر omissions.

### Phase 1 — سلامة سطح المكتب: 85% (المتبقي 15%)

- **منفذ:** حماية قاعدة التشفير، عزل مسارات الاختبار، propagation لأخطاء الحفظ، checkpoints باستخدام SQLite backup، journal لاستبدال قاعدة الاستعادة، integrity/FK checks.
- **الدليل:** `G:\b2b\src\main\services\StartupCoordinator.ts`، `G:\b2b\src\main\services\TenantPackageRestoreService.ts:186-286`.
- **النقص:** 3 اختبارات استعادة سطح مكتب فشلت في الجولة الحالية؛ لا توجد fault-injection ناجحة عند كل rename/fsync/commit؛ rollback متعدد المرفقات وقاعدة البيانات لم يُثبت في clean crash drill.
- **بوابة الإكمال:** إصلاح الاختبارات الثلاثة، ثم اختبارات قتل العملية عند كل مرحلة مع إثبات وجود قاعدة صالحة واحدة على الأقل بعد كل انقطاع.

### Phase 2 — العقد وسجل الكيانات: 75% (المتبقي 25%)

- **منفذ:** عقد مولد يحوي platform bindings، الحقول المطلوبة/nullable، FK، allowlists، الحساسية، الملكية، الترتيب وسياسة التعارض.
- **الدليل:** `src/shared/canonicalContract.ts:18-58` واختبارات coverage.
- **النقص الحاسم:** `contracts:check` يفشل لملفين مشتركين؛ generated artifacts ليست متطابقة في المستودعين. لا توجد خاصية صريحة لكل ما طلبته الخطة مثل schema version introduced وlegacy migration adapter لكل كيان.
- **بوابة الإكمال:** جعل `contracts:check` أخضر وإلزامه CI، ثم إضافة metadata المفقودة أو توثيق بديل مولد مكافئ.

### Phase 3 — حزمة `.b2btenant`: 70% (المتبقي 30%)

- **منفذ:** streaming archive، AEAD، hashes، path/size/count validation، manifest، staging، recovery passphrase، automation slot في v3، وقراءة v2.
- **الدليل:** `src/shared/streamingCrypto.ts`، `src/shared/recoveryArchive.ts`، `src/shared/b2btenant.ts`، اختبارات package/crypto.
- **النقص:** نسخ التشفير/الأرشيف غير متطابقة؛ full bidirectional round trips غير موجودة؛ لا يوجد clean different-machine proof؛ key rotation lifecycle ليس منفذاً كعملية تشغيلية.
- **بوابة الإكمال:** توحيد الملفات، ثم اختبارات حقيقية Web→package→Windows والعكس مع جميع الكيانات والمرفقات وتبديل هوية الجهاز.

### Phase 4 — Web export/import: 60% (المتبقي 40%)

- **منفذ:** tenant-scoped allowlists، upload sessions، staging، preview، confirmation token، safety backup، transaction، تحقق لاحق.
- **الدليل:** `cloud-server/src/routes/tenantBackup.ts`، `cloud-server/src/recovery/stagingStore.ts`، `cloud-server/src/recovery/postgresRestoreAdapter.ts`.
- **النقص:** اختبار PostgreSQL native للاستعادة المرحلية skipped في suite الافتراضية؛ جلسات الاستيراد في `Map` داخل العملية فلا تعمل بأمان عبر replicas؛ نتيجة COMMIT غير المؤكدة تتوقف عند حالة unknown بلا reconciliation؛ الاختبار الكامل من حزمة Windows لم ينجح.
- **بوابة الإكمال:** durable/shared import-session store، commit reconciliation، native restore في CI، واختبار tenant isolation متزامن بين مستأجرين.

### Phase 5 — Web Sync: 60% (المتبقي 40%)

- **منفذ:** pairing/revoke، allowlisted adapters، monotonic cursor، integer revision، idempotency بالشركة والعملية، conflicts، change-log triggers، transaction.
- **الدليل:** `cloud-server/src/routes/sync.ts`، `cloud-server/src/sync/syncPolicy.ts`، `cloud-server/src/sync/syncChangeCapture.ts`.
- **نقص حرج:** واجهة الويب تعيد نجاحاً زائفاً عند فشل الشبكة أو غياب النقل: `src/renderer/src/stores/sync.ts:25-44,67-116` و`src/renderer/src/api/ApiAdapter.ts:2480-2505`.
- **نقص إضافي:** delete ينفذ hard delete ثم يسجل tombstone (`sync.ts:131-134`) بلا سياسة احتفاظ/توزيع مثبتة؛ لا يوجد rate limiter ظاهر لمسارات sync؛ pairing لا يصدر بوضوح access token قصير العمر + refresh lifecycle مستقل.
- **بوابة الإكمال:** fail-closed UI، rate limits، device credential lifecycle، tombstone retention، واختبارات replay/revoke/expiry/out-of-order على PostgreSQL حقيقي.

### Phase 6 — مزامنة Windows الدائمة: 78% (المتبقي 22%)

- **منفذ:** جداول outbox/inbox/state/conflicts/devices/catalog/attachment/restore، triggers من السجل، SHA-256 وbase revision، applyPull، credentials في `safeStorage`، pairing وtoggle.
- **الدليل:** `G:\b2b\src\main\services\SyncStorage.ts`، `DesktopSyncTransport.ts`، `DesktopSyncConfiguration.ts`، `DesktopSyncService.ts`.
- **النقص:** لا يوجد إثبات شامل أن كل mutation خارج الجداول المسجلة يولد outbox؛ لا يظهر migration صريح لطابور localStorage القديم؛ full offline restart→server sync integration غير موجود؛ اختبارات سطح المكتب الكاملة ليست خضراء.
- **بوابة الإكمال:** mutation coverage instrumentation، localStorage migration test، وrestart integration على SQLite حقيقي ثم push/pull إلى PostgreSQL حقيقي.

### Phase 7 — Startup Coordinator: 65% (المتبقي 35%)

- **منفذ:** موصول فعلياً في `G:\b2b\src\main\index.ts:122` بناقل `DesktopSyncTransport`، integrity check، checkpoint عند التغيير، handshake وoffline behavior.
- **النقص:** لا توجد staging database مستقلة لكل pull؛ لا يوجد retry scheduler/backoff مثبت بعد فتح التطبيق؛ لا يوجد post-sync encrypted external backup enqueue؛ لا يوجد clean startup drill مع corrupt DB/server timeout/pending outbox في تطبيق packaged.
- **بوابة الإكمال:** تنفيذ البنود الثلاثة الناقصة وتشغيل matrix إقلاع حقيقية على نسخة packaged.

### Phase 8 — المرفقات: 62% (المتبقي 38%)

- **منفذ:** API رفع قابل للاستئناف، status/complete، hash/size verification، طابور SQLite، مسارات سطح مكتب محدودة بـ documents/cases/general مع realpath containment.
- **الدليل:** `cloud-server/src/routes/syncAttachments.ts`، `cloud-server/src/attachments/resumableAttachmentStore.ts`، `G:\b2b\src\main\services\AttachmentTransferQueue.ts`، `BoundedAttachmentPath.ts`.
- **النقص:** التنفيذ المحلي يفعّل فقط عبر `B2B_ENABLE_LOCAL_ATTACHMENT_SYNC=1` ومقيد localhost؛ download/resume/atomic final OfficeVault path غير مكتمل؛ طبقة تخزين web المعتمدة إنتاجياً غير مثبتة؛ metadata/tombstone وعلاقة parent الكاملة ليست دورة واحدة؛ لا يوجد network interruption test حقيقي.
- **بوابة الإكمال:** upload + download end-to-end، استئناف بعد قطع الشبكة، تخزين production مستقل، ورفض إعلان synced قبل hash نهائي.

### Phase 9 — Google Drive وOneDrive: 45% (المتبقي 55%)

- **منفذ:** `IndependentBackupStorage` محلي، Google Drive resumable endpoint مع `drive.file` والتحقق بإعادة التنزيل، OneDrive class وOAuth token refresh، اختبارات mock.
- **الدليل:** `cloud-server/src/recovery/independentStorage.ts`، `oneDriveIndependentStorage.ts`، `oAuthStorageAuth.ts`.
- **النقص:** Google الحالي يعتمد service-account config لا مسار OAuth تفاعلي مكتمل؛ OneDrive غير موصول بـ factory/CLI/policy؛ الرفع الكبير في OneDrive يرسل الملف دفعة واحدة بعد session وليس حلقة chunks قابلة للاستئناف؛ verification catalog غير موصول؛ لا يوجد periodic isolated download/decrypt verification؛ مجلد OneDrive المحلي المطلوب غير موصول كوجهة سياسة.
- **بوابة الإكمال:** ربط providers بالأوامر والسياسة والكتالوج، chunk loop قابل للاستئناف، OAuth UI/secure secret storage، وفحص دوري حقيقي على حساب اختبار فقط.

### Phase 10 — واجهات التعارض والتعافي: 45% (المتبقي 55%)

- **منفذ:** مؤشر مزامنة، pending/conflict counts، last sync، restore preview، recovery-key test، pairing dialog.
- **الدليل:** مكونات `src/renderer/src/components/sync/` و`Settings.vue` في المستودعين.
- **النقص:** لا توجد إدارة أجهزة كاملة list/revoke؛ لا تظهر latest verified backup وbackup destination state بصورة موثوقة؛ واجهة الويب قد تعرض synced كذباً؛ تقارير التحقق والمرفقات الناقصة ليست كاملة؛ لا توجد واجهة تهيئة متقدمة للسياسة/retention.
- **بوابة الإكمال:** ربط UI بحالات الخادم fail-closed واختبارات متصفح لجميع حالات الفشل والـ RTL.

### Phase 11 — أدوات DR: 70% (المتبقي 30%)

- **منفذ ومثبت:** `pg_dump` custom، manifest hash، backup/restore للمرفقات، رفض restore إلى DB غير فارغة، CLI مستقل، واختبار DR حي ناجح داخل Docker إلى PostgreSQL جديد مع القيود والصفوف والمرفق.
- **الدليل:** `cloud-server/src/recovery/postgresDisasterRecovery.ts`، `cloud-server/src/cli/postgres-dr.ts`، `cloud-server/src/__tests__/postgresDisasterRecovery.native.test.ts`.
- **النقص:** `encryptDumpFile` يشفر dump فقط ولا يغلف manifest + attachments كحزمة DR موحدة؛ لا يُستدعى تلقائياً من `dr:backup`; independent upload غير موصول بالـ CLI؛ retention الحالية keepLatest/maxAge فقط ولا تنفذ 14 daily/8 weekly/12 monthly ولا تفحص verified success؛ tenant-only export من infrastructure dump غير موجود كأداة مستقلة؛ smoke test للتطبيق بعد restore غير موجود.
- **بوابة الإكمال:** أمر واحد backup→encrypt-v3(two slots)→upload→verify→catalog→retention، وأمر restore عكسي، وأداة tenant extraction، واختبار app smoke.

### Phase 12 — التحقق والإطلاق: 25% (المتبقي 75%)

- **منفذ:** feature flags جزئية، runbook، Docker healthy، local push/pull يدوي سابق، وDR database drill حي.
- **النقص الحاسم:** لا يوجد shadow mode، test-office rollout موثق، clean Windows drill، owner-data isolated drill، أو rollback rehearsal كامل؛ العقود لا تتطابق؛ Web tests وDesktop tests ليست خضراء؛ لا توجد all-gates pipeline ثابتة.
- **بوابة الإكمال:** إغلاق كل النتائج أدناه ثم تشغيل clean-machine drill مع artifacts مستقلة، وإصدار تقرير قابل لإعادة التشغيل لا يعتمد على جلسة يدوية.

## 6. Definition of Done — الشروط الخمسة عشر

| # | الشرط | الحكم | الدليل أو سبب عدم القبول |
|---:|---|---|---|
| 1 | كل كيان أعمال في السجل | جزئي | coverage واسعة، لكن counts متباينة والعقود منجرفة |
| 2 | استحالة تسرب tenant في export/import/sync tests | جزئي | allowlists موجودة؛ لا توجد مصفوفة native كاملة لكل scope/replica |
| 3 | Web package يستعاد كلياً في Windows | غير منفذ كاختبار قبول | لا يوجد round-trip حقيقي ناجح بكل الكيانات والمرفقات |
| 4 | Windows package يستعاد كلياً في Web | غير منفذ كاختبار قبول | نفس الفجوة، ونسخ recovery archive غير متطابقة |
| 5 | counts/hashes/relations/attachment hashes متساوية | غير مثبت | DR المصغر يثبت مثالاً صغيراً فقط، لا tenant الكامل عبر التطبيقين |
| 6 | استعادة على Windows مختلف بلا identity/license | غير مثبت | crypto unit tests ليست clean-machine drill |
| 7 | تغييرات offline تنجو من restart ثم تتزامن | جزئي | SQLite outbox موجود؛ لا يوجد اختبار restart→real server كامل |
| 8 | startup لا يطغى على بيانات أحدث بصمت | جزئي | revision conflicts موجودة؛ لا يوجد packaged race test شامل |
| 9 | full restore له rollback point متحقق دائماً | جزئي | safety backup موجود؛ COMMIT unknown والتعطل المرحلي غير محسومين |
| 10 | automatic backups مشفرة ومخزنة مستقلاً | جزئي ضعيف | primitives موجودة لكنها غير موصولة كسياسة تلقائية كاملة |
| 11 | latest verified backup ظاهر للمستخدم | غير مكتمل | لا توجد واجهة موثوقة مربوطة بكتالوج التحقق |
| 12 | JSON snapshots القديمة قابلة للاستيراد | **مكتمل وظيفياً** | importer ما زال موجوداً واختبارات التوافق موجودة؛ يجب إبقاؤه في regression suite |
| 13 | الأسماء/العربية/migrations/تغييرات المستخدم سليمة | جزئي | لا rename/delete متعمد، لكن لا clean-upgrade + UI regression شامل |
| 14 | TS/ESLint/unit/integration/build تمر في المستودعين | **فشل** | Web 1 test failed، Desktop 3 tests failed، contract check failed |
| 15 | DR كامل: PG جديد + Windows نظيف + artifacts مستقلة | **فشل** | PG الجديد نجح، لكن clean Windows وindependent artifact drill لم ينفذا |

**النتيجة:** 1/15 مكتمل بثقة. بما أن الشروط تراكمية، حالة Definition of Done الكلية هي **غير مكتملة**.

## 7. تغطية متطلبات الاختبار 1–39

| المجموعة | الحالة |
|---|---|
| 1–4 دورات Web/Windows ذهاباً وإياباً | غير منفذة كاختبارات حقيقية كاملة |
| 5–6 جهاز مختلف/بدون license | غير منفذة عملياً |
| 7–16 passphrase/corruption/truncation/manifest/duplicate/traversal/version/entity/attachment/FK | توجد اختبارات وحدة جيدة لمعظمها؛ ليست كلها مثبتة داخل round-trip حقيقي |
| 17–18 disk full/process termination لكل restore stage | غير مكتملة |
| 19–20 network interruption upload/download | غير مكتملة؛ download نفسه ناقص |
| 21–26 token expiry/revoke/replay/out-of-order/delete/financial conflicts | replay/conflict جزئي؛ expiry/revoke/financial end-to-end غير مكتملة |
| 27 tenant isolation | اختبارات وسياسات موجودة؛ لا يوجد proof شامل لكل entity/scope على PostgreSQL حقيقي |
| 28 JSON v1 | موجود |
| 29 `.b2bpack` القديم | توافق جزئي بحسب قابلية فك التشفير، بلا matrix موثق |
| 30–33 startup offline/timeout/corrupt/pending | unit tests جزئية؛ لا packaged clean-machine drill |
| 34 registry coverage | موجود لكنه لا يغلق drift/count discrepancy |
| 35 TypeScript | نجح في Web/Cloud/Desktop |
| 36 ESLint | 0 errors، لكن warnings كثيرة جداً؛ البوابة لا تعتبر warnings فشلاً حالياً |
| 37 Unit tests | ليست خضراء بالكامل |
| 38 Integration tests | بعضها نجح؛ 3 Desktop فشلت، و2 Cloud skipped افتراضياً |
| 39 Production builds | نجحت للويب والخادم وسطح المكتب في هذه الجولة |

## 8. النتائج الحرجة الواجب إصلاحها بالترتيب

### P0 — تمنع أي إعلان نجاح

1. **إزالة النجاح الزائف في مزامنة الويب.** كل غياب/فشل للنقل يجب أن يعطي `failed/offline` و`success:false`، ولا يحدث تحديث `lastSyncAt` إلا بعد ACK وpull صالحين.
2. **توحيد العقود المشتركة.** إصلاح `streamingCrypto.ts` و`recoveryArchive.ts` حتى ينجح `contracts:check`، ثم جعله بوابة CI.
3. **إصلاح اختبارات Web/Desktop الفاشلة.** لا يجوز تصنيف suite ناجحاً من رمز PowerShell إذا أعلن Vitest فشلاً.
4. **تنفيذ round-trip الحقيقي في الاتجاهين** على PostgreSQL وSQLite مؤقتين مع المرفقات والحسابات والعلاقات.

### P1 — سلامة الاستعادة والمزامنة

5. إضافة reconciliation لحالة PostgreSQL `COMMIT outcome unknown` وتدقيق `indeterminate` مستقل بدلاً من ترك العملية معلقة بلا نتيجة قابلة للحسم.
6. استبدال `Map` لجلسات الاستيراد بمخزن durable/shared مع locking وTTL وnonce ذري.
7. إكمال download/resume للمرفقات والكتابة المؤقتة ثم hash ثم atomic rename إلى OfficeVault.
8. إقرار سياسة tombstone retention وعدم الاعتماد على hard delete غير المقيد.
9. إضافة token قصير العمر + refresh + revoke tests وrate limits لمسارات sync/attachments.
10. إكمال startup staging/backoff/post-sync encrypted backup.

### P2 — DR والوجهات الخارجية والإطلاق

11. تغليف **حزمة DR كاملة** (dump + manifest + attachment inventory/blobs) بـ streamingCrypto v3 ذي المنفذين، لا تشفير dump منفرد فقط.
12. ربط backup CLI بالتشفير والرفع المستقل والتحقق والكتالوج؛ وربط Google/OneDrive فعلياً خلف feature flags.
13. تنفيذ retention اليومية/الأسبوعية/الشهرية مع الحفاظ على أحدث نسخة verified وعدم الحذف قبل تحقق الأحدث.
14. تنفيذ tenant extraction من infrastructure backup كأداة مستقلة.
15. تشغيل shadow/test-office/clean-Windows drill، ثم بوابة واحدة تجمع contracts/typecheck/lint/tests/build/DR/E2E.

## 9. معيار الإغلاق المقبول

لا تُرفع النسبة إلى 100% ولا تغلق الخطة إلا إذا تحققت جميع النقاط التالية في تشغيل واحد قابل للتكرار:

1. `contracts:check` أخضر.
2. جميع typecheck/lint/tests/builds خضراء بلا حالات failed أو skipped إلزامية.
3. Web↔Windows round trips حقيقية ومتساوية في counts/hashes/FKs/attachments.
4. clean Windows different-identity restore ناجح بلا license أصلي.
5. offline restart sync وconflict/delete/revoke/expiry/network interruption مثبتة.
6. DR artifact كاملة ومشفرة v3 ومرفوعة مستقلاً، ثم منزلة ومستعادة إلى PostgreSQL جديد.
7. retention وlatest verified backup UI مثبتتان.
8. shadow rollout وrollback rehearsal موثقان.

حتى ذلك الحين، الوصف الصحيح للحالة هو:

> **تنفيذ جزئي متقدم، صالح لمواصلة التطوير والاختبار المحلي، لكنه غير مكتمل وغير جاهز للإطلاق أو لادعاء حماية بيانات بنسبة 100%.**
