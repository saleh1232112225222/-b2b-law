You are the primary implementation agent responsible for building a secure, loss-resistant backup, restore, portable tenant export, and offline synchronization system across two existing B2B-LAW applications.

A separate Codex agent is the technical supervisor. You are the implementer. At the end of every phase, you must stop and provide a supervisor report. Do not continue to the next phase until the user confirms that the Codex supervisor has approved it.

======================================================================
PROJECTS
======================================================================

Desktop Windows application:
- Repository: G:\b2b
- Stack: Electron, Vue 3, TypeScript, better-sqlite3/SQLite
- Local file vault: OfficeVault
- Existing formats:
  - manual JSON snapshots
  - encrypted .b2bpack packages
- Existing data may be real and sensitive.

Web application:
- Repository: G:\w2w
- Stack:
  - Vue 3 / TypeScript frontend
  - Express / TypeScript backend
  - PostgreSQL
- Multi-tenant system using company_id
- Deployed environment exists on Render/Netlify, but production must not be touched without explicit approval.

Local external backup destination currently used by the owner:
- C:\Users\saleh\OneDrive\bass

Preferred future independent remote backup provider:
- Google Drive using a separate account from the hosting provider.

======================================================================
PRIMARY OBJECTIVE
======================================================================

Implement a system that:

1. Preserves all existing business data, identifiers, Arabic names, schema naming, migrations, encryption behavior, UI behavior, and existing user changes.

2. Keeps backward compatibility with existing manual JSON snapshots.

3. Creates a new encrypted, portable, cross-machine office package:
   - .b2btenant

4. Supports complete office export and recovery between:
   - PostgreSQL web application
   - SQLite Windows desktop application

5. Synchronizes both applications without transferring raw PostgreSQL or SQLite files between them.

6. Automatically performs safe backup and differential synchronization at application startup.

7. Never performs a destructive full snapshot injection automatically at startup.

8. Works offline on Windows, queues local changes durably, and synchronizes them when connectivity returns.

9. Includes all business entities and real attachment bytes without silently omitting any table or entity.

10. Prevents cross-tenant data leakage under every circumstance.

11. Provides verifiable, tested recovery on a completely different Windows device.

======================================================================
NON-NEGOTIABLE SAFETY RULES
======================================================================

Before editing anything:

1. Read all AGENTS.md, repository instructions, package scripts, migration conventions, and existing test conventions in both repositories.

2. Run and record:
   - git status
   - current branch
   - current commit
   - relevant uncommitted files

3. Both repositories may contain valuable uncommitted user changes.
   - Never reset them.
   - Never discard them.
   - Never use git reset --hard.
   - Never use git checkout -- on user files.
   - Never overwrite unrelated edits.
   - Never clean untracked files.

4. Do not inspect or output real customer records, credentials, tokens, .env values, database contents, recovery secrets, or attachment content.

5. Do not run tests against real userData, production PostgreSQL, Render, Netlify, Google Drive, or the owner’s OneDrive folder.

6. All tests must use isolated temporary databases and temporary file vaults.

7. Do not deploy, push, create a pull request, access production services, or modify live databases without explicit user approval.

8. Do not create local commits unless explicitly approved. Maintain a precise change log instead.

9. Migrations must be:
   - additive first
   - idempotent
   - reversible where practical
   - safe on existing installations
   - tested with old and new schemas

10. No schema/table/column rename or deletion is allowed unless separately approved after a compatibility audit.

11. Do not install a new dependency without first reporting:
   - why it is needed
   - its license
   - its maintenance status
   - security implications
   - a dependency-free alternative

12. Never report success when:
   - a mandatory entity failed
   - an attachment is missing
   - encryption failed
   - verification failed
   - only part of a transaction was applied

13. Every destructive restore must:
   - create a verified pre-restore backup
   - require explicit confirmation
   - run in staging first
   - have a tested rollback path

======================================================================
MANDATORY ARCHITECTURE
======================================================================

The web application is the central authority while online.

The Windows application is an offline-first client that:
- stores a complete local office working set
- queues local changes in SQLite
- pushes changes when online
- pulls server changes using a monotonic server cursor
- resolves conflicts without silently overwriting newer data

Do not directly synchronize database files.

Use a canonical logical data contract:

PostgreSQL
  ⇅ adapters
Versioned B2B Data Contract
  ⇅ adapters
SQLite

Create a versioned, machine-readable contract shared by both repositories.

Do not introduce a runtime dependency on the sibling repository.

Recommended approach:
- Maintain a canonical versioned JSON Schema/data-contract artifact.
- Generate TypeScript types and validators for both repositories.
- Include contractId and contractHash.
- Add tests in both repositories that reject an incompatible contract.
- Generated artifacts must not be edited manually.

======================================================================
CURRENT KNOWN DESKTOP RISKS THAT MUST BE ADDRESSED
======================================================================

Audit and fix these issues in G:\b2b:

1. Development startup must never delete the encrypted .enc database when decryption fails.

2. Development userData must be isolated from real office userData.

3. persistEncryptedDatabase currently may log and swallow failures.
   - Encryption/persistence failure must propagate to the caller.
   - The UI must not show success after a persistence failure.

4. Existing .b2bpack restore uses a limited preferred table list.
   - The package may contain a full SQLite database, but restore injects only selected tables.
   - Employees, legal services, office management, payment, notification, and other entities may be omitted.
   - Replace hard-coded partial coverage with the canonical Entity Registry.

5. Existing backup encryption is tied to machine identity.
   - New portable packages must not depend on machine ID or license state.

6. Database restore and OfficeVault restore are not one atomic recoverable operation.
   - Introduce staging, verification, commit, and rollback.

7. Export currently builds large buffers in memory and writes directly to the final file.
   - Use streaming.
   - Write to a temporary file.
   - flush/fsync where available.
   - verify the completed package.
   - atomically rename to the final destination.

8. Validate package size, part count, duplicate entries, manifest size, allowed paths, and attachment paths.

9. The manual JSON snapshot is plaintext and does not include real attachment bytes.
   - Preserve its importer for compatibility.
   - Clearly classify it as a legacy logical snapshot.
   - New exports must use the secure portable package.

10. The existing automatic logout snapshot is not a real scheduler.
    - Do not call it a complete disaster backup.

11. The current sync queue uses localStorage.
    - Move it to SQLite.

12. Queue operations are not wired into all repository mutations.

13. Pulled sync changes are currently not reliably applied to SQLite.

14. Desktop preload/IPC does not provide a complete hardened sync implementation.

15. Cloud restore must stop if its pre-restore backup is cancelled or fails.

======================================================================
CURRENT KNOWN WEB RISKS THAT MUST BE ADDRESSED
======================================================================

Audit and fix these issues in G:\w2w:

1. /system/export-snapshot dynamically enumerates PostgreSQL tables.
   - Replace this with an explicit allowlisted Entity Registry.
   - Never export another company’s data.
   - Never export global infrastructure/admin tables to a tenant package.

2. Tables without company_id must not automatically be exported globally.
   - Shared reference data must be explicitly classified and versioned.
   - Infrastructure tables must be excluded.

3. Snapshot import currently uses dynamic table processing.
   - Only registered entities and registered fields may be imported.
   - Unknown tables and unknown fields must be rejected or reported.

4. Import must validate:
   - formatVersion
   - schemaVersion
   - contractId
   - tenant identity
   - row counts
   - hashes
   - foreign keys
   - required fields
   - attachment manifest

5. Current sync routes use dynamic entity_type, table names, and field names.
   - Do not interpolate user-controlled identifiers into SQL.
   - Use a fixed server-side entity registry and per-entity adapters.

6. Remove fallback company/user UUID behavior.
   - Missing authenticated companyId or userId must fail closed.

7. Pull synchronization must use a monotonic server cursor, not only timestamps.

8. Operation idempotency must be scoped by:
   - company_id
   - operation_id

9. Use an integer revision or change sequence, not a date passed as base_revision.

10. Sync batches must be validated, size-limited, rate-limited, and transactional.

11. Do not silently catch sync errors and return “synced” or empty success results in production.

12. Attachments currently need a real binary synchronization mechanism.

13. Google Sheets synchronization must not be used for confidential legal data.
    - Do not expand or rely on the unauthenticated Google Apps Script bridge.
    - Keep it disabled for production legal data unless separately redesigned and approved.

======================================================================
CANONICAL ENTITY REGISTRY
======================================================================

Create one complete Entity Registry covering every business entity in both systems.

Each entry must define:

- canonical entity name
- PostgreSQL table/adapter
- SQLite table/adapter
- primary key
- tenant key
- stable UUID behavior
- field schema
- nullable fields
- sensitive fields
- immutable fields
- dependency order
- foreign key relationships
- deletion strategy
- conflict strategy
- attachment relationships
- export policy
- import policy
- sync policy
- schema version introduced
- migration adapter from legacy formats

The registry must cover all current business areas, including at minimum:

- companies/offices
- users
- roles and permissions
- employees
- clients
- defendants
- cases
- case parties
- case assignments
- sessions
- session outcomes
- judgments
- judgment amendments
- tasks
- task notifications
- task audit records
- documents
- evidence
- memoranda
- experts
- agencies
- communications
- legal service categories
- legal service types
- legal service statuses
- legal service priorities
- legal engagements
- consultation service details
- litigation service details
- contract service details
- legal service notes
- legal service timeline
- legal service attachments
- accounts
- client accounts
- finances
- finances_new
- invoices
- invoice items
- vouchers
- receivables
- credit notes
- payment schedules
- payment history
- enforcement files
- enforcement parties
- enforcement actions
- enforcement requests and detail tables
- collections claims and payments
- contracts
- contract templates
- contract parties
- contract participants
- contract signatures
- contract schedules
- contract links
- contract amendments
- contract audits
- partners
- partner contributions
- office expenses
- office budgets
- profit distributions
- firm data
- appointments
- professional liability logs
- file asset metadata
- any other current business table discovered during inventory

Create a coverage test that fails whenever:
- a business table exists without a registry entry
- a registry entry references a missing table/adapter
- a mandatory field has no mapping
- a dependency cycle cannot be resolved
- a business entity is silently omitted

======================================================================
PORTABLE .b2btenant PACKAGE
======================================================================

Implement a new cross-machine office package.

Suggested structure:

manifest.json
data/<entity>.json or streamed records
attachments/index.json
attachments/<content-hash>
audit/export-report.json

The manifest must contain:

- formatVersion
- schemaVersion
- contractId
- contractHash
- sourceApplication
- sourceApplicationVersion
- tenantId
- exportId
- createdAt
- backupType: full or incremental
- baseExportId for incrementals
- entity counts
- per-entity hashes
- attachment count
- attachment total size
- per-attachment SHA-256
- encryption algorithm
- KDF algorithm and parameters
- key slot metadata
- package signature metadata

Requirements:

1. All records must have stable identifiers.

2. All paths must be logical paths.
   - Never serialize C:\ or machine-specific absolute paths.

3. Attachment blobs must be content-addressed by SHA-256.

4. Reject:
   - path traversal
   - duplicate entries
   - oversized manifests
   - excessive part counts
   - unsupported versions
   - invalid Unicode paths
   - hash mismatches
   - truncated encrypted streams

5. Import into staging first.

6. Produce a preview before destructive replacement.

7. Verify post-import counts, hashes, relations, and attachment availability.

8. Existing JSON v1 import must remain supported through an explicit v1-to-v2 adapter.

======================================================================
ENCRYPTION AND KEY RECOVERY
======================================================================

Use envelope encryption.

1. Generate a random 256-bit Data Encryption Key for each package.

2. Encrypt package content using authenticated encryption:
   - AES-256-GCM or another approved AEAD mode
   - unique nonce/IV handling
   - streaming/chunk-safe format

3. Support at least two key slots for the same DEK:

   Recovery slot:
   - KEK derived from an office recovery passphrase
   - use scrypt or Argon2id with explicit stored parameters
   - never store the passphrase
   - allows recovery on a completely different device

   Automation slot:
   - desktop: OS secure storage/DPAPI-protected key
   - server: an approved secret manager/KMS-style key
   - allows unattended automatic backups

4. The recovery slot must remain sufficient when:
   - the original Windows device is destroyed
   - the license file is missing
   - Render is unavailable
   - the original hosting account is unavailable

5. Do not derive package keys from:
   - machine ID
   - installation path
   - license key
   - tenant ID alone
   - hard-coded constants

6. Add key versioning and rotation support.

7. Never log:
   - keys
   - passphrases
   - decrypted package contents
   - authentication tokens

8. Authentication/account secrets:
   - Do not synchronize live sessions, refresh tokens, OTP secrets, or reset tokens.
   - Preserve stable user identity and relationships.
   - Cross-platform account recovery should force secure password reset where password formats differ.

======================================================================
DESKTOP SQLITE SYNC STORAGE
======================================================================

Add additive SQLite migrations for:

sync_outbox
sync_state
sync_conflicts
sync_devices
sync_inbox
backup_catalog
attachment_transfer_queue
restore_runs

sync_outbox must include:

- local id
- operation_id
- tenant_id
- entity_type
- entity_id
- operation
- base_revision
- payload
- content_hash
- created_at
- retry_count
- next_retry_at
- status
- last_error

Every business create/update/delete operation must write its outbox event inside the same SQLite transaction as the business change.

Do not delete an outbox event until the server confirms the exact operation_id.

Do not use localStorage as the durable source of truth for synchronization.

Provide a safe migration path for any existing localStorage queue.

======================================================================
WEB SYNCHRONIZATION STORAGE
======================================================================

Use additive PostgreSQL migrations for:

- registered_sync_devices
- tenant_change_log
- sync_operations
- sync_conflicts
- sync_cursors
- backup_catalog
- attachment_transfer_state
- restore_runs

Requirements:

1. Device pairing must require an authenticated office administrator.

2. Each device must be individually revocable.

3. Access tokens must be short-lived.

4. Refresh credentials must be stored securely on Windows.

5. Every server change must generate a monotonic tenant change sequence.

6. Pull must use:
   - afterCursor
   - bounded page size
   - nextCursor
   - hasMore

7. Do not use the client clock to decide completeness.

8. Push must provide:
   - operationId
   - entityType
   - entityId
   - operation
   - baseRevision
   - payload
   - deviceId

9. The server must:
   - validate entity and fields
   - force company_id from authenticated context
   - detect replay/idempotency
   - detect revision conflict
   - apply inside a transaction
   - write audit and change-log entries
   - return the authoritative revision

10. Deletes must use tombstones for synchronized entities.

11. Tombstones must remain long enough for all authorized devices to receive them.

======================================================================
AUTOMATIC STARTUP COORDINATOR
======================================================================

Implement safe startup synchronization for the Windows application.

Startup sequence:

1. Acquire single-instance lock.

2. Initialize secure key access.

3. Inspect the local database without modifying it.

4. Run SQLite integrity verification.

5. If local integrity fails:
   - do not delete files
   - do not create a replacement silently
   - enter recovery mode
   - present verified restore options

6. If the local database changed since the last verified backup:
   - create a local atomic pre-start checkpoint

7. Open a staging database for incoming changes.

8. Attempt authenticated server handshake with a short timeout.

9. If offline:
   - open normally in offline mode
   - preserve the outbox
   - schedule retries with backoff

10. If online:
   - push pending outbox operations
   - pull server changes by monotonic cursor
   - validate them in staging
   - resolve or quarantine conflicts
   - apply changes transactionally
   - verify the final local state

11. Open the UI even if sync is unavailable, unless local integrity itself failed.

12. After successful synchronization:
   - create a post-sync checkpoint
   - enqueue an encrypted external backup upload

Never auto-import a complete JSON or complete .b2btenant package during ordinary startup.

Full package restore must always be an explicit recovery operation.

======================================================================
ATTACHMENT SYNCHRONIZATION
======================================================================

Implement real binary attachment synchronization.

Every attachment must have:

- stable attachment UUID
- tenant ID
- parent entity type
- parent entity ID
- logical filename
- content type
- byte size
- SHA-256
- created_at
- updated_at
- revision
- deletion/tombstone state

Requirements:

1. Desktop stores blobs in OfficeVault.

2. Web stores blobs in an independent approved file storage layer.

3. Transfer only blobs missing by hash.

4. Upload/download must be resumable where practical.

5. Download to a temporary file.

6. Verify byte size and SHA-256.

7. Atomically move to the final vault path.

8. Do not mark an attachment as synchronized until verification succeeds.

9. A restore report must list every missing or failed attachment.

10. Required attachments must prevent a “fully successful” restore result when absent.

======================================================================
GOOGLE DRIVE AND LOCAL EXTERNAL BACKUPS
======================================================================

Google Drive is an independent encrypted backup target, not a live database.

Implement behind a feature flag.

Requirements:

1. Use OAuth authorization.

2. Request the narrowest practical scope, preferably drive.file.

3. Upload only already encrypted .b2btenant packages and encrypted infrastructure backup artifacts.

4. Use resumable upload.

5. Persist:
   - Drive file ID
   - export ID
   - content hash
   - size
   - upload status
   - last verified time

6. Verify remote metadata after upload.

7. Periodically download a selected backup into isolated temporary storage and verify:
   - package authentication
   - manifest
   - hashes
   - decryptability using the configured recovery method

8. Never upload plaintext manual JSON automatically.

9. Support the existing folder:
   - C:\Users\saleh\OneDrive\bass
   as an optional local external backup destination.

10. When writing to OneDrive:
   - write a temporary file
   - verify it
   - atomically rename where supported
   - do not interpret local existence as proof that cloud sync completed

11. Do not connect to the owner’s real Google Drive or OneDrive during implementation tests.

======================================================================
BACKUP POLICY
======================================================================

Implement configurable policy with safe defaults:

- At startup:
  - create a checkpoint only when data changed

- After successful synchronization:
  - create an incremental package

- During use:
  - persist the outbox immediately
  - create a checkpoint on a safe interval when dirty

- At shutdown:
  - create a checkpoint when unsaved durable changes exist
  - never block shutdown indefinitely

- Daily:
  - encrypted full tenant package

- Weekly:
  - independent PostgreSQL custom-format dump
  - attachment inventory/package

- Monthly:
  - retained archive copy

Default retention proposal:

- 14 daily backups
- 8 weekly backups
- 12 monthly backups
- never remove the most recent verified successful backup
- do not delete an old backup before a newer backup passes verification

Retention deletion must be narrowly scoped and thoroughly tested.

======================================================================
POSTGRESQL INFRASTRUCTURE BACKUP
======================================================================

Keep infrastructure disaster recovery separate from tenant portability.

Implement scripts/tools for:

1. pg_dump custom-format PostgreSQL backup.

2. Attachment storage backup and hash inventory.

3. Independent encrypted upload.

4. Full restore into a new empty PostgreSQL environment.

5. Restore verification:
   - schema/migration state
   - constraints
   - row counts
   - tenant counts
   - attachment counts
   - hashes
   - application smoke tests

6. Tenant-only export from an infrastructure backup must remain a separate tool.

The normal tenant package must never contain data for another tenant.

======================================================================
CONFLICT POLICIES
======================================================================

Implement explicit policies:

General master data:
- revision-based conflict detection
- automatic merge only for non-overlapping field changes
- same-field conflicts require user review

Delete versus update:
- never silently choose one
- create a conflict

Financial records:
- no last-write-wins
- approved financial records are immutable
- correction/reversal operations must be appended and audited

Contracts, signatures, judgments, and approved legal documents:
- never silently merge approved content
- require explicit conflict resolution

Every resolution must record:
- resolver user
- device
- timestamp
- selected strategy
- before/after hashes
- affected entity

======================================================================
UI REQUIREMENTS
======================================================================

Preserve current application styling, Arabic RTL behavior, desktop/mobile responsiveness, light/dark themes, names, and routes.

Add minimal UI for:

- online/offline state
- synchronization progress
- pending operation count
- unresolved conflict count
- last verified backup
- last successful sync
- backup destination state
- restore preview
- restore verification report
- device management
- recovery key setup
- recovery key confirmation test

Do not expose technical stack traces or secrets to users.

Do not claim:
- “complete”
- “fully backed up”
- “successfully restored”
unless all mandatory verification gates passed.

======================================================================
TESTING REQUIREMENTS
======================================================================

Use behavior-based tests with isolated temporary data.

Mandatory tests:

1. Complete Web → package → Windows round trip.

2. Complete Windows → package → Web round trip.

3. Web → Windows → Web with:
   - equal entity counts
   - equal canonical hashes
   - equal attachment hashes

4. Windows → Web → Windows with the same verification.

5. Restore on a completely different machine identity.

6. Restore without the original license file.

7. Wrong recovery passphrase.

8. Corrupted package.

9. Truncated encrypted stream.

10. Modified manifest.

11. Duplicate archive entries.

12. Path traversal attempts.

13. Unsupported contract version.

14. Missing mandatory entity.

15. Missing attachment.

16. Foreign key mismatch.

17. Database full/disk full simulation.

18. Process termination at each restore stage.

19. Network interruption during upload.

20. Network interruption during download.

21. Expired access token.

22. Revoked device.

23. Duplicate operation replay.

24. Out-of-order operations.

25. Delete/update conflict.

26. Financial conflict.

27. Tenant isolation:
    - a tenant must never read, export, import, modify, or infer another tenant’s records

28. Old JSON v1 import compatibility.

29. Old .b2bpack compatibility where technically decryptable.

30. Startup while offline.

31. Startup with server timeout.

32. Startup with a corrupt local database.

33. Startup with pending outbox events.

34. Schema registry coverage for every business table.

35. TypeScript typecheck.

36. ESLint.

37. Unit tests.

38. Integration tests.

39. Production builds for both applications.

Do not use mocks as the only proof of restore correctness.

At least one suite must use real temporary SQLite and PostgreSQL test databases.

======================================================================
PHASED IMPLEMENTATION
======================================================================

PHASE 0 — Baseline and safety inventory
- Read instructions.
- Record repository states.
- Inventory all schemas, migrations, entities, attachments, and current backup/sync code.
- Establish isolated test directories/databases.
- Produce the complete entity coverage matrix.
- Do not modify production logic yet.

PHASE 1 — Desktop critical safety fixes
- Prevent encrypted database deletion on failure.
- Isolate development userData.
- Propagate persistence failures.
- Correct pre-restore backup cancellation behavior.
- Add atomic local backup primitives.
- Add regression tests.

PHASE 2 — Canonical data contract and Entity Registry
- Create versioned contract.
- Create generated validators/types.
- Create full table coverage tests.
- Create explicit dependency ordering.
- Preserve legacy adapters.

PHASE 3 — Portable .b2btenant package
- Streaming package creation.
- Cross-machine envelope encryption.
- Manifest and hashes.
- Attachment inclusion.
- Staged verification.
- Legacy JSON compatibility.

PHASE 4 — Web tenant export/import
- Explicit tenant-scoped export.
- Staging import.
- Preview and verification.
- Transactional apply and rollback.
- No global table leakage.

PHASE 5 — Hardened web sync API
- Registered devices.
- Allowlisted entities/fields.
- Monotonic cursors.
- Integer revisions.
- Idempotent operations.
- Tombstones.
- Conflicts.
- Transactional audit log.

PHASE 6 — Durable desktop synchronization
- SQLite outbox/inbox/state/conflicts.
- Wire every repository mutation.
- Apply pulled changes.
- Remove localStorage as durable source.
- Add offline retry.

PHASE 7 — Startup coordinator
- Integrity check.
- Pre-start checkpoint.
- push/pull/staging/apply.
- offline behavior.
- post-sync backup.

PHASE 8 — Attachment synchronization
- Content-addressed hashes.
- resumable transfers.
- atomic local writes.
- verification.

PHASE 9 — Google Drive and OneDrive backup targets
- Feature flagged.
- encrypted artifacts only.
- resumable upload.
- verification catalog.
- isolated restore verification.

PHASE 10 — Conflict and recovery UI
- Sync state.
- conflict review.
- restore preview.
- device control.
- recovery key workflow.

PHASE 11 — Disaster recovery and tenant-export tooling
- PostgreSQL full backup tool.
- attachment backup tool.
- standalone restore tool.
- tenant-export tool.
- restore without original application frontend.

PHASE 12 — Full verification and controlled rollout
- Shadow mode first.
- Test office.
- Copy of owner data in isolated environment only after explicit approval.
- Feature flags.
- Recovery drill on clean machine.
- Documentation and rollback runbook.

======================================================================
SUPERVISOR GATE FORMAT
======================================================================

At the end of each phase, stop and output exactly:

SUPERVISOR GATE — PHASE <number>

1. Outcome
2. Files changed
3. Migrations added
4. Existing behavior preserved
5. Tests executed
6. Exact test results
7. Typecheck/lint/build results
8. Data-loss risks considered
9. Security risks considered
10. Remaining risks
11. Rollback procedure
12. Uncommitted user changes preserved
13. Diff summary
14. Decisions requiring supervisor approval
15. Proposed next phase

Do not proceed until approval is explicitly returned.

If any test fails:
- do not hide it
- do not weaken the test merely to make it pass
- report the root cause
- fix only within the authorized phase
- rerun the complete relevant suite

======================================================================
DEFINITION OF DONE
======================================================================

The work is complete only when:

1. Every business entity is covered by the registry.

2. No tenant data leakage is possible in export/import/sync tests.

3. A tenant package created by the web app restores completely into Windows.

4. A tenant package created by Windows restores completely into the web app.

5. All row counts, canonical hashes, relationships, and attachment hashes match.

6. Recovery works on a different Windows device without original machine identity or license files.

7. Offline Windows changes survive restart and synchronize later.

8. Startup synchronization cannot overwrite newer data silently.

9. Full restore always has a verified rollback point.

10. Automatic backups are encrypted and independently stored.

11. The latest verified backup status is visible to the user.

12. Existing JSON snapshots remain importable.

13. Existing naming, Arabic UI, migrations, and unrelated user changes remain intact.

14. TypeScript, ESLint, unit tests, integration tests, and production builds pass in both repositories.

15. A complete disaster recovery drill succeeds using:
    - a new PostgreSQL environment
    - a clean Windows environment
    - independently stored backup artifacts

Begin with PHASE 0 only.

Do not edit application logic during PHASE 0.

After completing the inventory and risk analysis, stop at:

SUPERVISOR GATE — PHASE 0