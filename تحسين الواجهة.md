# خطة إصلاحات الواجهة البصرية قبل الإطلاق

> تاريخ الإعداد: 30 يونيو 2026
> الحالة: جاهز للتنفيذ

---

## الإصلاح 1: عنوان "ClientProfile" بالإنجليزية

**المشكلة:** عنوان الصفحة يظهر "ClientProfile" بدلاً من "ملف الموكل"

**الملف المُصَلَّح:** `src/renderer/src/App.vue`

**التغيير المطلوب:** إضافة `ClientProfile: 'ملف الموكل'` إلى `nameMap` في سطر ~302

```typescript
// قبل
const nameMap: Record<string, string> = {
    Dashboard: 'لوحة التحكم',
    Clients: 'إدارة الموكلين',
    Cases: 'القضايا والملفات',
    Sessions: 'الجلسات والتقويم',
    Tasks: 'المهام والتذكيرات',
    Finance: 'المالية والمحاسبة',
    Documents: 'الأرشفة والمستندات',
    Memoranda: 'المذكرات واللوائح',
    Contracts: 'العقود',
    Enforcement: 'التنفيذ والتحصيل',
    Profile: 'الملف الشخصي'
}

// بعد
const nameMap: Record<string, string> = {
    Dashboard: 'لوحة التحكم',
    Clients: 'إدارة الموكلين',
    ClientProfile: 'ملف الموكل',        // <-- إضافة جديدة
    Cases: 'القضايا والملفات',
    Sessions: 'الجلسات والتقويم',
    Tasks: 'المهام والتذكيرات',
    Finance: 'المالية والمحاسبة',
    Documents: 'الأرشفة والمستندات',
    Memoranda: 'المذكرات واللوائح',
    Contracts: 'العقود',
    Enforcement: 'التنفيذ والتحصيل',
    Profile: 'الملف الشخصي'
}
```

---

## الإصلاح 2: تكبير أزرار اللمس (Touch Target)

**المشكلة:** الأزرار أصغر من الحد الأدنى المطلوب (44px) مما ي صعّب النقر على الهواتف

**الملف المُصَلَّح:** `src/renderer/src/assets/mobile.css`

**التغيير المطلوب:** إضافة قواعد CSS بعد سطر 27

```css
/* إضافة جديدة */
.mobile-app-shell .v-btn {
  min-height: 44px !important;
}

.mobile-app-shell .v-btn.block,
.mobile-app-shell .v-btn[variant="elevated"][block] {
  min-height: 48px !important;
}

.mobile-app-shell .v-tab {
  min-height: 44px !important;
}

.mobile-app-shell .v-list-item {
  min-height: 48px !important;
}
```

---

## الإصلاح 3: حجم خطوط BottomNav صغير جداً

**المشكلة:** حجم خط `.nav-label` هو `0.65rem` وهو صغير جداً على الهواتف الصغيرة

**الملف المُصَلَّح:** `src/renderer/src/assets/mobile.css`

**التغيير المطلوب:** تعديل `.nav-label` في سطر 70

```css
/* قبل */
.nav-label {
  font-size: 0.65rem;
  font-weight: 700;
  color: var(--text-muted);
  line-height: 1;
}

/* بعد */
.nav-label {
  font-size: 0.7rem;
  font-weight: 700;
  color: var(--text-muted);
  line-height: 1;
  white-space: nowrap;
}
```

---

## الإصلاح 4: Safe Area مكرر في Header

**المشكلة:** في `mobile.css` سطر 89-97، `mobile-header` يعرّف نفسه مرتين مما يسبب تعارض CSS

**الملف المُصَلَّح:** `src/renderer/src/assets/mobile.css`

**التغيير المطلوب:** حذف التعريف المكرر في سطر 89-92

```css
/* حذف هذا الكود المكرر */
.mobile-header {
  height: var(--mobile-header-height) !important;
  min-height: var(--mobile-header-height) !important;
}

/* إبقاء فقط التعريف الصحيح */
.mobile-header {
  padding-top: env(safe-area-inset-top);
  height: calc(var(--mobile-header-height) + env(safe-area-inset-top));
}
```

---

## الإصلاح 5: تحسين شاشة تحميل ملف العميل

**المشكلة:** شاشة التحميل صغيرة وتحتوي على spinner فقط

**الملف المُصَلَّح:** `src/renderer/src/components/mobile/MobileClientProfile.vue`

**التغيير المطلوب:** تعديل الكود في سطر 2-5

```vue
<!-- قبل -->
<v-card v-if="loading" elevation="0" class="glass-panel-light pa-6 text-center">
  <v-progress-circular indeterminate color="accent" size="48"></v-progress-circular>
  <div class="mt-4 text-gold opacity-40 font-weight-black">جاري التحميل...</div>
</v-card>

<!-- بعد -->
<v-card v-if="loading" elevation="0" class="glass-panel-light pa-8 text-center">
  <v-progress-circular indeterminate color="accent" size="56" width="4"></v-progress-circular>
  <div class="mt-6 text-body-1 text-gold opacity-60 font-weight-black">جاري تحميل بيانات الموكل...</div>
</v-card>
```

---

## الإصلاح 6: تقليل معلومات بطاقة العميل

**المشكلة:** بطاقة العميل تعرض معلومات كثيرة مما يسبب ازدحام

**الملف المُصَلَّح:** `src/renderer/src/components/mobile/MobileClients.vue`

**التغيير المطلوب:** تعديل `info-fields` في سطر 19-22

```vue
<!-- قبل -->
:info-fields="[
  { key: 'type', label: 'النوع' },
  { key: 'city', label: 'المدينة' }
]"

<!-- بعد -->
:info-fields="[
  { key: 'type', label: 'النوع' }
]"
```

---

## الإصلاح 7: إضافة Pull-to-Refresh لقوائم البطاقات

**المشكلة:** `usePullToRefresh` موجود لكن غير مُستخدَم

**الملف المُصَلَّح:** `src/renderer/src/components/mobile/MobileCardList.vue`

**التغيير المطلوب:** ربط `usePullToRefresh` بـ `containerRef`

في `<script setup>` بعد السطر 112 أضف:

```typescript
import { usePullToRefresh } from '../../composables/usePullToRefresh'

// في defineEmits أضف refresh
const emit = defineEmits<{
  'item-click': [item: any]
  add: []
  refresh: []
}>()

// بعد containerRef
const { isRefreshing, pullDistance } = usePullToRefresh(containerRef, async () => {
  emit('refresh')
})
```

---

## الإصلاح 8: إضافة Pull-to-Refresh للداشبورد

**الملف المُصَلَّح:** `src/renderer/src/components/mobile/MobileDashboard.vue`

**التغيير المطلوب:** استقبال حدث `refresh` من `MobileCardList`

```vue
<!-- في MobileCardList داخل الجلسات -->
@refresh="refreshSessions"

<!-- في MobileCardList داخل المعاملات -->
@refresh="refreshTransactions"
```

وإضافة الدوال في `<script setup>`:

```typescript
const refreshSessions = async () => {
  await sessionsStore.listSessions({})
}

const refreshTransactions = async () => {
  await financeStore.fetchFinanceData()
}
```

---

## الإصلاح 9: إضافة اسماً عربياً لصفحة الجلسات

**الملف المُصَلَّح:** `src/renderer/src/App.vue`

**التغيير المطلوب:** التأكد من وجود Sessions في nameMap (موجود مسبقاً)

التحقق فقط من أن `Sessions: 'الجلسات والتقويم'` موجود في `nameMap`.

---

## ملخص التغييرات

| # | الملف | التعديل | الأولوية |
|---|-------|---------|----------|
| 1 | `App.vue` | إضافة `ClientProfile` لخريطة الأسماء | حرجة |
| 2 | `mobile.css` | تكبير أزرار `min-height: 44px` | عالية |
| 3 | `mobile.css` | تكبير حجم خطوط BottomNav | عالية |
| 4 | `mobile.css` | إصلاح Safe Area التكراري | عالية |
| 5 | `MobileClientProfile.vue` | تحسين شاشة التحميل | متوسطة |
| 6 | `MobileClients.vue` | تقليل معلومات البطاقة | عالية |
| 7 | `MobileCardList.vue` | ربط Pull-to-Refresh | متوسطة |
| 8 | `MobileDashboard.vue` | إضافة refresh للقوائم | متوسطة |

---

## أوامر التنفيذ بعد التعديل

```bash
npm run lint    # فحص الأخطاء النحوية
npm run build   # التأكد من بناء المشروع
```

---

_انتهت الخطة. جاهز للتنفيذ._
