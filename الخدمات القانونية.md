# دليل المطور وموجه التنفيذ (Developer Guide & Prompt)
## وحدة الخدمات والارتباطات القانونية (الخطة المعتمدة والمنفذة فعلياً)

> آخر تحديث: 2026-06-27

هذا المستند هو الموجه الفني المعتمد لمطور تطبيقات الويب والهاتف المحمول لإعادة بناء وتكرار نفس البنية البرمجية والميزات التي تم دمجها واختبارها بنجاح في تطبيق النظام الرئيسي.

---

## أولاً: البنية التحتية وقاعدة البيانات (Database Schema)

تعتمد وحدة الخدمات على **نظام البيانات المعزول والمرجعي** لضمان عدم وجود قيم ثابتة (Hardcoded Values) في الأكواد. يتم إنشاء الجداول المرجعية وتعبئتها أولاً، تليها جداول العمليات والمساعدات.

### 1. جداول المراجع (Reference Lookup Tables)

#### أ. تصنيفات الخدمات الرئيسية (`legal_service_categories`)
* `id` (TEXT PRIMARY KEY - UUID)
* `key` (TEXT UNIQUE NOT NULL - معرف فريد)
* `name_ar` (TEXT NOT NULL - الاسم بالعربية)
* `name_en` (TEXT - الاسم بالإنجليزية)

#### ب. أنواع الخدمات التفصيلية (`legal_service_types`)
* `id` (TEXT PRIMARY KEY - UUID)
* `category_id` (TEXT NOT NULL - ربط مع جدول التصنيفات)
* `key` (TEXT UNIQUE NOT NULL - معرف فريد)
* `name_ar` (TEXT NOT NULL - الاسم بالعربية)
* `name_en` (TEXT - الاسم بالإنجليزية)
* *العلاقة*: `FOREIGN KEY (category_id) REFERENCES legal_service_categories(id) ON DELETE CASCADE`

#### ج. حالات الخدمات القانونية (`legal_service_statuses`)
* `id` (TEXT PRIMARY KEY - UUID)
* `status_key` (TEXT UNIQUE NOT NULL - مفتاح الحالة: pending, in_progress, completed, cancelled)
* `status_name_ar` (TEXT NOT NULL - الاسم بالعربية)
* `status_name_en` (TEXT - الاسم بالإنجليزية)
* `color` (TEXT - لون الوسم كـ Chip في الواجهات)

#### د. أولويات الخدمات (`legal_service_priorities`)
* `id` (TEXT PRIMARY KEY - UUID)
* `priority_key` (TEXT UNIQUE NOT NULL - مفتاح الأولوية: low, medium, high, urgent)
* `priority_name_ar` (TEXT NOT NULL - الاسم بالعربية)
* `priority_name_en` (TEXT - الاسم بالإنجليزية)
* `color` (TEXT - لون الوسم)

---

### 2. جدول العمليات الأساسي (`legal_engagements`)
هذا هو الجدول المحوري للارتباطات والخدمات القانونية. **جميع العلاقات مع الجداول الأخرى اختيارية (Nullable) لحماية النظام عند الحذف الإداري**.

* `id` (TEXT PRIMARY KEY - UUID)
* `engagement_number` (TEXT UNIQUE NOT NULL - الرقم المرجعي للخدمة)
* `category_id` (TEXT NOT NULL - ربط مع التصنيفات الرئيسية)
* `engagement_type_id` (TEXT NOT NULL - ربط مع أنواع الخدمات التفصيلية)
* `client_id` (TEXT NULL - العميل المرتبط - اختياري)
* `beneficiary` (TEXT NULL - المستفيد في حال اختلف عن العميل)
* `linked_parties` (TEXT NULL - الأطراف المرتبطون بالخدمة)
* `responsible_lawyer_id` (TEXT NULL - المحامي المسؤول - اختياري)
* `assistant_team` (TEXT NULL - الفريق المساعد)
* `description` (TEXT NULL - وصف الخدمة)
* `purpose` (TEXT NULL - الغرض)
* `start_date` (TEXT NULL - تاريخ البدء)
* `expected_end_date` (TEXT NULL - تاريخ الانتهاء المتوقع)
* `completion_date` (TEXT NULL - تاريخ الإنجاز الفعلي)
* `status_id` (TEXT NOT NULL - ربط مع الحالات المرجعية)
* `priority_id` (TEXT NOT NULL - ربط مع الأولويات المرجعية)
* `payment_method` (TEXT NULL - طريقة السداد)
* `contract_id` (TEXT NULL - العقد المرتبط - اختياري)
* `case_id` (TEXT NULL - القضية المرتبطة - اختياري)
* `invoice_id` (TEXT NULL - الفاتورة المرتبطة - اختياري، يُعبأ تلقائياً عند إصدار فاتورة)
* `created_at` (DATETIME DEFAULT CURRENT_TIMESTAMP)
* `created_by` (TEXT - منشئ السجل لتدقيق الحسابات)
* `updated_at` (DATETIME DEFAULT CURRENT_TIMESTAMP)
* `updated_by` (TEXT)
* `deleted_at` (DATETIME NULL - حقل الحذف الناعم)
* `deleted_by` (TEXT NULL)

**العمليات المالية المدمجة (حسابات السيرفر فقط):**
* `financial_compensation` (REAL DEFAULT 0 - المقابل المالي)
* `tax` (REAL DEFAULT 0 - الضريبة)
* `paid_amount` (REAL DEFAULT 0 - المبلغ المسدد)
* `remaining_amount` (REAL DEFAULT 0 - المبلغ المتبقي - **يتم احتسابه وتحديثه في الخلفية/السيرفر فقط** بالمعادلة: `(المقابل + الضريبة) - المسدد`).

---

### 3. جداول البيانات المساعدة والتمديد (Auxiliary Tables)

#### أ. جداول التفاصيل المتخصصة حسب نوع الخدمة

* **`consultation_service_details`** - تفاصيل الاستشارات
  * `engagement_id` (TEXT PRIMARY KEY - ربط بالخدمة)
  * `consultation_medium` (TEXT - 'شفوي' | 'مكتوب')
  * `legal_opinion_summary` (TEXT - ملخص الرأي القانوني)

* **`litigation_service_details`** - تفاصيل التقاضي
  * `engagement_id` (TEXT PRIMARY KEY - ربط بالخدمة)
  * `court_level` (TEXT - مستوى المحكمة)
  * `opponent_details` (TEXT - بيانات الخصم)

* **`contract_service_details`** - تفاصيل العقود
  * `engagement_id` (TEXT PRIMARY KEY - ربط بالخدمة)
  * `contract_scope` (TEXT - نطاق العقد)
  * `drafting_language` (TEXT - لغة الصياغة)

#### ب. مرفقات الخدمة القانونية (`legal_service_attachments`)
* `id` (TEXT PRIMARY KEY - UUID)
* `engagement_id` (TEXT NOT NULL - ربط بالخدمة الأساسية)
* `file_name` (TEXT NOT NULL - اسم الملف)
* `file_path` (TEXT NOT NULL - مسار التخزين الآمن)
* `uploaded_by` (TEXT)
* `uploaded_at` (DATETIME DEFAULT CURRENT_TIMESTAMP)
* *العلاقة*: `FOREIGN KEY (engagement_id) REFERENCES legal_engagements(id) ON DELETE CASCADE`

#### ج. الملاحظات الداخلية للمحامين (`legal_service_notes`)
* `id` (TEXT PRIMARY KEY - UUID)
* `engagement_id` (TEXT NOT NULL - ربط بالخدمة)
* `note_text` (TEXT NOT NULL - نص الملاحظة)
* `created_by` (TEXT)
* `created_at` (DATETIME DEFAULT CURRENT_TIMESTAMP)
* *العلاقة*: `FOREIGN KEY (engagement_id) REFERENCES legal_engagements(id) ON DELETE CASCADE`

#### د. سجل التغييرات والخط الزمني للخدمة (`legal_service_timeline`)
* `id` (TEXT PRIMARY KEY - UUID)
* `engagement_id` (TEXT NOT NULL - ربط بالخدمة)
* `event_type` (TEXT NOT NULL - نوع الحدث: create, update, delete, note_added, attachment_uploaded)
* `event_title` (TEXT NOT NULL - عنوان الحدث بالعربية)
* `event_description` (TEXT NULL - تفاصيل التغيير)
* `actor` (TEXT - المستخدم الذي أجرى العملية)
* `created_at` (DATETIME DEFAULT CURRENT_TIMESTAMP)
* *العلاقة*: `FOREIGN KEY (engagement_id) REFERENCES legal_engagements(id) ON DELETE CASCADE`

---

## ثانياً: البيانات المرجعية الواجب بذرها (Seeding Data)

### 1. حالات الخدمة الافتراضية (`legal_service_statuses`)
* `pending` (قيد الانتظار - اللون: برتقالي/orange)
* `in_progress` (قيد العمل - اللون: أزرق/blue)
* `completed` (منجزة - اللون: أخضر/green)
* `cancelled` (ملغاة - اللون: أحمر/red)

### 2. أولويات الخدمة الافتراضية (`legal_service_priorities`)
* `low` (منخفضة - اللون: أخضر/green)
* `medium` (متوسطة - اللون: أزرق/blue)
* `high` (مرتفعة - اللون: برتقالي/orange)
* `urgent` (عاجلة - اللون: أحمر/red)

### 3. التصنيفات الـ 16 والخدمات التابعة لها (`categories` & `types`)
*(ملاحظة: تم دمج الأحوال الشخصية تماماً لتقتصر على التقاضي العام كخدمة كافية، وإدراج "خدمة أخرى" في خدمات أخرى)*:
* **الاستشارات**: [استشارة شفوية، استشارة مكتوبة، رأي قانوني، فتوى نظامية]
* **التقاضي**: [رفع دعوى، الدفاع في دعوى، الاعتراض على حكم، الاستئناف، النقض، التماس إعادة النظر، التنفيذ]
* **العقود**: [إعداد عقد، مراجعة عقد، تعديل عقد، ترجمة عقد، توثيق عقد]
* **الشركات**: [تأسيس شركة، تعديل عقد تأسيس، تحويل كيان، زيادة رأس المال، تخفيض رأس المال، تصفية شركة، حوكمة]
* **التنفيذ**: [طلب تنفيذ، منازعة تنفيذ، إيقاف تنفيذ، رفع إيقاف، متابعة التنفيذ]
* **التوثيق**: [وكالة، إقرار، مصالحة، مخالصة، تعهد، إنذار، محضر اتفاق]
* **الملكية الفكرية**: [تسجيل علامة، اعتراض على علامة، نقل ملكية علامة، ترخيص استخدام، حقوق مؤلف]
* **العقار**: [إفراغ، بيع، شراء، تقسيم، فرز، نزاعات عقارية، مراجعة صكوك]
* **العمل**: [صياغة عقد عمل، لائحة تنظيم العمل، فصل موظف، مطالبات عمالية، تسوية عمالية]
* **التجارة**: [وكالة تجارية، امتياز تجاري، توزيع، تحصيل ديون، إفلاس، إعادة تنظيم مالي]
* **التحكيم**: [اتفاق تحكيم، تمثيل في التحكيم، تنفيذ حكم تحكيم]
* **الوساطة**: [وساطة، تفاوض، تسوية، صلح]
* **الامتثال**: [سياسات، لوائح داخلية، مكافحة غسل الأموال، حماية البيانات، الحوكمة]
* **الجهات الحكومية**: [استخراج تراخيص، الاعتراضات الإدارية، التظلمات، متابعة الطلبات]
* **الترجمة القانونية**: [ترجمة العقود، ترجمة الأحكام، ترجمة المذكرات]
* **خدمات أخرى**: [إنذار عدلي، مذكرة قانونية، خطاب رسمي، دراسة نظامية، بحث قانوني، خدمة أخرى]

---

## ثالثاً: التكامل المالي (Financial Integration) — **محدّث**

### 1. المزامنة التلقائية مع السجل المالي (`syncFinanceRecord`)
عند **إنشاء أو تعديل** خدمة قانونية يتم تلقائياً:
* إذا كان `paid_amount > 0`: إنشاء أو تحديث سجل في جدول `finances` بنوع `income` وتصنيف `خدمات قانونية`
* إذا كان `paid_amount = 0`: حذف السجل المالي المرتبط (إن وجد)
* عند **الحذف الناعم** للخدمة: يتم حذف السجل المالي المرتبط من `finances`

**الملفات المرجعية:**
* `LegalServiceRepository.ts` → دالة `syncFinanceRecord()`
* `LegalServiceRepository.ts` → دالة `create()` (سطر 386-395)
* `LegalServiceRepository.ts` → دالة `update()` (سطر 488-499)
* `LegalServiceRepository.ts` → دالة `softDelete()` (سطر 552)

### 2. إصدار الفواتير الضريبية (`generateInvoice`)
يتيح إصدار فاتورة ضريبية تلقائياً لأي خدمة قانونية:
* يتم إنشاء سجل في جدول `invoices` مع حساب الحالة (`paid`, `partially_paid`, `unpaid`)
* يتم إنشاء بند في جدول `invoice_items` بوصف الخدمة وقيمتها
* يتم ربط الفاتورة بالخدمة عبر تحديث حقل `invoice_id` في `legal_engagements`
* يتم تسجيل الحدث في الخط الزمني وسجل النشاط

**الملفات المرجعية:**
* `LegalServiceRepository.ts` → دالة `generateInvoice()` (سطر 578-658)
* `LegalServices.vue` → دالة `handleGenerateInvoice()` (سطر 1011)

### 3. التقارير المالية والميزانية (`getBudgetStats`)
دالة متقدمة في `FinanceRepository` توفر:
* إجمالي الدخل والمصروفات خلال فترة زمنية محددة
* الرصيد الصافي (الفرق بين الدخل والمصروفات)
* تفصيل المصروفات حسب التصنيف
* فلترة بتاريخ البداية والنهاية والتصنيف

**الملفات المرجعية:**
* `FinanceRepository.ts` → دالة `getBudgetStats()` (سطر 191-208)
* `FinanceRepository.ts` → دالة `getStats()` (سطر 157-178)

### 4. جدول تصنيفات المصروفات (`expense_categories`)
جدول مرجعي للمصروفات مع بيانات مبذورة افتراضياً:
* رواتب | إيجار | مرافق | تسويق | مستحقات قانونية | مستلزمات مكتبية

**الملفات المرجعية:**
* `migrateExpenseCategories.ts` → ملف الترحيل (migration)
* `database.ts` → استدعاء `migrateExpenseCategories(db)` (سطر 208)

---

## رابعاً: ربط المهام بالخدمات القانونية — **محدّث**

### نوع الربط `link_type: 'legal_service'`
تم توسيع نظام المهام (`tasks_v2`) لدعم ربط المهام بالخدمات القانونية:

* واجهة `Task` تدعم الأنواع: `'none' | 'case' | 'client' | 'legal_service'`
* عند إنشاء مهمة بنوع `legal_service`:
  * يتم تعيين `linkType = 'legal_service'`
  * يتم تعيين `ownerType = 'client'`
* في استعلامات العرض، يتم عرض سياق المهمة: `خدمة قانونية: [رقم الخدمة]`

**الملفات المرجعية:**
* `TaskRepository.ts` → واجهة `Task` (سطر 9)
* `TaskRepository.ts` → دالة `create()` (سطر 323-325)
* `TaskRepository.ts` → استعلام `context_label` (سطر 256)

---

## خامساً: الواجهات والربط التفاعلي للعميل (Frontend Vue & Mobile)

1. **النموذج الذكي (LegalServiceForm)**:
   * عند فتح النموذج، يتم جلب المراجع بالكامل من السيرفر.
   * عند اختيار المستخدم "التصنيف الرئيسي" (مثلاً: الاستشارات)، يتم فلترة قائمة "اسم الخدمة" ديناميكياً لتعرض فقط الخدمات التابعة للاستشارات.
   * المبالغ المالية: يتيح إدخال المقابل، الضريبة، والمسدد. ويقوم النموذج بعرض "المتبقي" تلقائياً كحساب استرشادي للواجهة، بينما السيرفر يقوم بالعملية الحسابية وتخزينها عند الحفظ لضمان الحماية والـ Security.
2. **شاشة الخدمات الرئيسية (LegalServices)**:
   * توفر إحصائيات علوية (إجمالي الخدمات، قيد العمل، منجزة، ومجموع الرسوم غير المحصلة).
   * جدول يعرض الخدمات مع إتاحة البحث، والفلترة بالتصنيف أو الحالة المرجعية.
   * **زر إصدار فاتورة ضريبية** — يقوم بإنشاء فاتورة تلقائياً وربطها بالخدمة.
   * زر أرشفة/حذف ناعم يقوم بالاستدعاء لوسم السجل بـ `deleted_at`.
   * زر تفاصيل يعرض نافذة منبثقة بها 4 تبويبات:
     1. *البيانات العامة*: حقول الخدمة بالكامل مع العقود والقضايا والفواتير المرتبطة.
     2. *الملاحظات*: قائمة الملاحظات الداخلية من جدول `legal_service_notes` وإتاحة إضافة ملاحظة.
     3. *المرفقات*: قائمة الملفات المرفوعة، مع إمكانية استدعاء مستعرض الملفات لرفع ملف جديد وحفظ مساره في جدول `legal_service_attachments`.
     4. *الخط الزمني (Timeline)*: يعرض بالتسلسل التاريخي خط الأحداث التلقائي لتأسيس وتعديل هذه الخدمة.
3. **لوحة التحكم الرئيسية (Dashboard)**:
   * جلب الإحصائيات الشاملة من السيرفر (والتي تضم الآن كائن `legalServices` المضاف حديثاً).
   * في حال كانت الخاصية مفعلة من الإعدادات، يتم عرض كارت إحصائي KPI تفاعلي إضافي يعرض عدد الخدمات الحالية في أعلى لوحة التحكم لربط البرنامج بالكامل كـ ERP موحد.

---

## سادساً: الملفات المصدرية الرئيسية (Source Files Map)

| الملف | الوظيفة |
|-------|---------|
| `src/main/db/legalServicesSchema.ts` | إنشاء الجداول وبذر البيانات المرجعية |
| `src/main/db/repositories/LegalServiceRepository.ts` | جميع عمليات CRUD والتكامل المالي وإصدار الفواتير |
| `src/main/db/repositories/FinanceRepository.ts` | الإحصائيات المالية وتقارير الميزانية |
| `src/main/db/repositories/TaskRepository.ts` | ربط المهام بالخدمات القانونية |
| `src/main/db/migrations/migrateExpenseCategories.ts` | ترحيل تصنيفات المصروفات |
| `src/main/db/migrations/migrateFinance.ts` | ترحيل السجل المالي الموحد |
| `src/main/db/database.ts` | تسجيل واستدعاء الترحيلات في `ensureCriticalSchema` |
| `src/main/ipc/handlers.ts` | نقاط IPC للتواصل بين الواجهة والسيرفر |
| `src/renderer/src/views/LegalServices.vue` | واجهة عرض وإدارة الخدمات القانونية |
| `src/renderer/src/views/Settings.vue` | إعدادات النظام ومزامنة ناجز |

---

## سابعاً: سجل التغييرات (Changelog)

### 2026-06-27
* ✅ إصلاح خطأ TypeScript: إضافة `'legal_service'` إلى نوع `link_type` في واجهة `Task`
* ✅ إصلاح خطأ TypeScript: معامل `username` غير مستخدم في `syncFinanceRecord` → `_username`
* ✅ إصلاح خطأ TypeScript: متغير `invId` غير مستخدم في `LegalServices.vue`
* ✅ إضافة جدول `expense_categories` مع بيانات مبذورة (رواتب، إيجار، مرافق، تسويق، إلخ)
* ✅ إضافة دالة `getBudgetStats()` في `FinanceRepository` لتقارير الميزانية بفترة زمنية
* ✅ تسجيل ترحيل `migrateExpenseCategories` في `database.ts`
* ✅ نجاح البناء الكامل (typecheck:node + typecheck:web) بدون أخطاء
